# Normalize line endings to LF in the repo (deterministic cross-platform checkout)
* text=auto eol=lf

# Treat these as binary (no eol munging, no diff noise)
*.safetensors binary
*.xlsx binary
*.gz binary
*.svg text eol=lf
